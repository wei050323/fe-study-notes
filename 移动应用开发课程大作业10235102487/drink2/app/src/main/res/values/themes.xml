<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. 使用 Material 组件 -->
    <style name="Base.Theme.Drink2" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/page_background</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- 兼容旧属性，保证主色一致 -->
        <item name="colorPrimary">@color/coffee_primary</item>
        <item name="colorSecondary">@color/milk_tea_primary</item>
    </style>

    <style name="Theme.Drink2" parent="Base.Theme.Drink2" />
</resources>